# Build dependencies

MonstaTek Core builds with ESP-IDF v6.0.1 and the framework components supplied
by that SDK, including FreeRTOS, NimBLE, and mbedTLS. These dependencies are
provided by the configured build toolchain and are not vendored into this
repository.
